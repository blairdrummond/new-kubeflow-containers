# This workflow will build a docker container, publish it to Azure Container Registry, and deploy it to Azure Kubernetes Service using a helm chart.
#
# https://github.com/Azure/actions-workflow-samples/tree/master/Kubernetes
#
# To configure this workflow:
#
# 1. Set up the following secrets in your workspace:
#     a. REGISTRY_USERNAME with ACR username
#     b. REGISTRY_PASSWORD with ACR Password
#     c. AZURE_CREDENTIALS with the output of `az ad sp create-for-rbac --sdk-auth`
#
# 2. Change the values for the REGISTRY_NAME, CLUSTER_NAME, CLUSTER_RESOURCE_GROUP and NAMESPACE environment variables (below).
name: build
on:
  push:
    branches:
      - master

# Environment variables available to all jobs and steps in this workflow
env:
  # REGISTRY_NAME: k8scc01covidacr
  REGISTRY_NAME: blairdrummond
  CLUSTER_NAME: k8s-cancentral-01-covid-aks
  CLUSTER_RESOURCE_GROUP: k8s-cancentral-01-covid-aks
jobs:
  build:
    strategy:
      matrix:
        notebook:
          - RStudio
          - JupyterLab-CPU
          - JupyterLab-PyTorch
          - JupyterLab-Tensorflow
          - VSCode-CPU
          - VSCode-PyTorch
          - VSCode-Tensorflow
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    # Connect to Azure Container registry (ACR)
    #- uses: azure/docker-login@v1
    #  with:
    #    # login-server: ${{ env.REGISTRY_NAME }}.azurecr.io
    #    login-server: ${{ env.REGISTRY_NAME }}.azurecr.io
    #    username: ${{ secrets.REGISTRY_USERNAME }}
    #    password: ${{ secrets.REGISTRY_PASSWORD }}

    - name: Free disk space
      run: |
        sudo swapoff -a
        sudo rm -f /swapfile
        sudo apt clean
        docker rmi $(docker image ls -aq)
        df -h

    # Container build and push to a Azure Container registry (ACR)
    - name: Build image
      run: |
        sudo apt-get install --yes make
        make all
        cd output/${{ matrix.notebook }}
        echo docker build . -t $(echo ${{ matrix.notebook }} | tr '[:upper:]' '[:lower:]')
        docker build . -t $(echo ${{ matrix.notebook }} | tr '[:upper:]' '[:lower:]')
        echo docker build -f base-notebook/cpu/Dockerfile -t ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${{ github.sha }} base-notebook/cpu
        echo docker push ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${{ github.sha }}
        echo docker tag ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${{ github.sha }} ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${GITHUB_REF#refs/*/}
        echo docker push ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${GITHUB_REF#refs/*/}
        docker system prune -f -a
        cd -

   #  # Scan image for vulnerabilities
   #  - uses: Azure/container-scan@v0
   #    with:
   #      image-name: ${{ env.REGISTRY_NAME }}.azurecr.io/base-notebook-cpu:${{ github.sha }}
   #      severity-threshold: CRITICAL
   #      run-quality-checks: false
